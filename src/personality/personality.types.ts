export type PersonalityState = {
  formality: number; // 0 = casual | 1 = formal
  verbosity: number; // 0 = curto | 1 = longo
  curiosity: number; // 0 = não pergunta | 1 = pergunta muito
  intimacy: number; // cresce com o tempo
};
